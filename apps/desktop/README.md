# OpenAgent Desktop App

Planned home for the Electron desktop application:

- Electron main process
- preload bridge
- React renderer
- desktop packaging configuration

Current status: desktop source has moved to `apps/desktop/src`. Root-level package scripts still orchestrate build/dev/package during the incremental migration.
