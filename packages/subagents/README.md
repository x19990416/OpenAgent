# @openagent/subagents

System subagent package for OpenAgent runtime: shell agent, knowledge agent, and Pi coding child agent tooling.

The package owns subagent task parsing/execution and tool definitions. Hosts inject provider-specific runtime adapter creation for the Pi coding child agent so this package does not depend on Electron desktop internals.
