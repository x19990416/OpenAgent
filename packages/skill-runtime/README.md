# @openagent/skill-runtime

Skill discovery, parsing, relevance resolution, prompt construction, install helpers, audit logging, and managed skill tool definitions. Desktop hosts this package through `SkillService` and still owns IPC/tool registry integration.

Build with:

```bash
pnpm --filter @openagent/skill-runtime build
```
