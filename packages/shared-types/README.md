# @openagent/shared-types

Planned package for stable shared contracts:

- IPC request/response types
- UI event DTOs
- runtime public DTOs
- plugin public types

Current source lives in `packages/shared-types/src`. The root renderer build still references it through the existing `@shared-types/*` alias during the incremental migration.
