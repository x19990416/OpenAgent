# @openagent/knowledge

OpenAgent knowledge runtime package. It owns the system wiki provider, knowledge service, knowledge tools, LLM-assisted routing/helpers, and knowledge storage paths. Desktop injects host logging via `configureKnowledgeLoggerHooks`.

Build with:

```bash
pnpm --filter @openagent/knowledge build
```
