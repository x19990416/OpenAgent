# @openagent/pi-adapter

Pi AgentSession adapter package. It owns Pi model/auth/session/tool/event glue while desktop-specific policy, settings, logging, and approval wiring stay injected by `apps/desktop`.

Build with:

```bash
pnpm --filter @openagent/pi-adapter build
```
