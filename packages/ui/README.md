# @openagent/ui

Reusable React UI package for the OpenAgent desktop renderer.

This package contains presentational workbench components, renderer UI helper functions, and renderer view-model types. Desktop-only composition, Electron API access, hooks, and app bootstrapping stay in `apps/desktop/src/renderer`.
