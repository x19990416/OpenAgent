# @openagent/plugin-runtime

Plugin runtime package for manifest types, registry discovery, config state, dynamic plugin loading, plugin context registration, and runtime event/prompt bridge.

Desktop still owns host-specific integration such as Electron secret storage, dependency installation, authorization browser flows, and IPC wiring.

Build with:

```bash
pnpm --filter @openagent/plugin-runtime build
```
