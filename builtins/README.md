# Builtins

Built-in resources that ship with OpenAgent live here after migration.

Planned:

- `skills/`: built-in skills
- `plugins/`: built-in plugins, if any

Current built-in skills still live under the root `skills/` directory.
