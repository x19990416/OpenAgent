# OpenAgent Docs

This directory contains the public GitHub documentation for OpenAgent.

In the source GitLab repository, these files live under `github-docs/` and are published to `docs/` in the GitHub `OpenAgent` repository by `scripts/publish-github-openagent.sh`.

The source repository's internal `docs/` directory is intentionally not published to GitHub by that script.
