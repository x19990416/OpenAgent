# Markdown Report Style

- Prefer short headings and bullet points.
- Keep the first section actionable.
- Use fenced code blocks only for commands, config, or exact snippets.
- Avoid over-formatting; the output should paste cleanly into GitHub/GitLab Markdown.
